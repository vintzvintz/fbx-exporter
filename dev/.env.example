# shellcheck shell=bash disable=all
# Development Environment Configuration for FBX Exporter
# Copy this file to .env and customize for your setup

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================

# Docker Compose project name (optional)
COMPOSE_PROJECT_NAME=fbx-exporter-dev

# =============================================================================
# APPLICATION VERSIONS
# =============================================================================

PROMETHEUS_VERSION=v2.54.1
GRAFANA_VERSION=11.3.0

# =============================================================================
# BIND CONFIGURATION
# =============================================================================

# IP address to bind services to
# Default: 127.0.0.1 (localhost only)
# Use 0.0.0.0 to bind to all interfaces (external access)
# Use specific IP for single interface binding
BIND_IP=127.0.0.1

# =============================================================================
# USER CONFIGURATION (Development)
# =============================================================================

# User/Group IDs for containers to avoid permission issues
# Use your user ID to match file ownership: id -u && id -g
# Default: 65534 (nobody) for Prometheus, 472 (grafana) for Grafana
DEV_UID=1000
DEV_GID=1000

# =============================================================================
# PORTS CONFIGURATION
# =============================================================================

# Prometheus
PROMETHEUS_PORT=9090

# Grafana
GRAFANA_PORT=3000

# =============================================================================
# GRAFANA CONFIGURATION
# =============================================================================

# Grafana credentials are configured via Docker secrets in:
# - .secrets/grafana_admin_user.txt
# - .secrets/grafana_admin_password.txt
# Variables d'environnement pour credentials non supportées dans Grafana 11.x

# =============================================================================
# PROMETHEUS CONFIGURATION
# =============================================================================

# Data retention period
PROMETHEUS_RETENTION_TIME=730d

# Global scrape intervals
PROMETHEUS_SCRAPE_INTERVAL=15s
FREEBOX_SCRAPE_INTERVAL=30s

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================

DOCKER_NETWORK_NAME=fbx-export-dev

# =============================================================================
# DATA PATHS (Development only)
# =============================================================================

# Prometheus data directory
PROMETHEUS_DATA_PATH=./data/prometheus

# Grafana data directory
GRAFANA_DATA_PATH=./data/grafana

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================

# 1. freebox-exporter runs on the host machine, not in Docker
# 2. Start with: go run . .secrets/token.json
# 3. Prometheus scrapes from host.docker.internal:9091
# 4. Use Docker secrets for Grafana credentials
# 5. Data is persisted in ./data/ directories
# 6. Set DEV_UID/DEV_GID to your user ID for proper file permissions
#    Check with: id -u && id -g